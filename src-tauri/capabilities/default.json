{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the first-party OpenHistory windows",
  "windows": ["compact", "history"],
  "permissions": ["core:default"]
}
